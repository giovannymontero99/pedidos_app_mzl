import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pedidos_app_mzl',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
